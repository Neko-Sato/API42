[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "API42"
version = "0.1"
dependencies = [
  "httpx",
  "beautifulsoup4"
]

[tool.setuptools]
packages = ["API42"]
